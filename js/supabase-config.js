// Supabase project credentials.
//
// Local development: replace the two placeholders below with your real project
// values from Supabase → Project Settings → API, but don't commit that change —
// this file is regenerated at deploy time from GitHub repository secrets
// (SUPABASE_URL, SUPABASE_ANON_KEY) by .github/workflows/deploy.yml.
//
// The "anon" key is safe to expose in client-side code — it can only do what your
// Row Level Security policies (see schema.sql) allow it to do. Keeping it out of
// the committed source here is a config-hygiene practice, not a security fix.
window.SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';
window.SUPABASE_ANON_KEY = 'YOUR-ANON-PUBLIC-KEY';
